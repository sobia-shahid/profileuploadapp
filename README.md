React app for user to simply upload and edit profile picture and user-name.
user have to authenticate before user can showcase his/her profile.

To install dependencies,

$ npm install
To run the project itself,

$ npm start
